CREATE TABLE Plants (
    plant_id INTEGER PRIMARY KEY,
    name TEXT,
    description TEXT,
    watering_frequency TEXT,
    humidity_exposure TEXT,
    temperature_range TEXT,
    date_added DATE
);